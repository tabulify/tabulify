
dependencies {
  implementation(project(":bytle-db"))
  implementation(project(":bytle-web-document"))
  testImplementation(project(":bytle-http"))
}

description = "Web document in a Tabular data path format"
