# Grafana

## Directory
This directory has the export of dashboard (Share > Export)
created with Grafana cloud.

## Data

Data to the prometheus data store of Grafana cloud is
send by Prometheus.

See the write section in [](../templates/prometheus.yml)
