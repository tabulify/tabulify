---
# handlers file for nexus

- name: 'nexus reload'
  become: yes
  systemd:
    name: 'nexus.service'
    enabled: 'yes'
    daemon_reload: yes
    state: restarted

- name: reload systemctl
  become: yes
  systemd: daemon_reload=yes