description = "A Bytle client framework - This library permits you to define and parse the argument that an utility tool will expect."

dependencies {
  compile(project(":bytle-log"))
  testImplementation(project(":bytle-fs"))
}
