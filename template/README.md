# Template Engine API

This repository has a template API that allows to use multiple engine:

* env var text engine
* json template
* pebble template
* thymeleaf template


See the [tests](src/test/java/com/tabulify/template) for examples.
