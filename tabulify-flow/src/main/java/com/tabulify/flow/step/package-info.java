/**
 * The operations.
 *
 * They implements the flow interface the stream functional interface that can be used in a stream
 *
 *   * The stream creation can be started with {@link com.tabulify.flow.stream.DataPathStream#createFrom(DataPathSupplier)}
 *   * The
 *
 * by passing one {@link com.tabulify.flow.stream.DataPathSupplier}
 *
 */
package com.tabulify.flow.step;

import com.tabulify.flow.stream.DataPathSupplier;
